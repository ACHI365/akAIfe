{
  "name": "akaife-travel-advisor",
  "version": "1.0.0",
  "description": "AI-powered travel advisor with Georgian Railway integration and location services",
  "scripts": {
    "dev": "concurrently \"./start-backend.sh\" \"./start-frontend.sh\"",
    "start:backend": "./start-backend.sh",
    "start:frontend": "./start-frontend.sh",
    "build": "cd akaife-front && npm run build",
    "install:all": "cd akaife-back && npm install && cd ../akaife-front && npm install && cd .. && uv sync",
    "test:health": "curl -s http://localhost:5000/health"
  },
  "keywords": ["ai", "travel", "advisor", "react", "typescript", "node", "python", "anthropic"],
  "author": "Nika Rusishvili",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
